export default class GameLayout {
  // Layout of the default game board
  constructor(environment) {
    this.environment = environment;

    this.tempEndGame =[85, 2, 100, 40];
    this.endTurn =[112, 40, 225, 50];
  }
}
